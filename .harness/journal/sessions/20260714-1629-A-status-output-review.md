# Status output review

**Goal.** Describe appropriate fields and boundaries for the proposed `time-arc-service status` output without changing product behavior.

**What happened.** Reviewed the proposed CLI schema and legacy Windows status implementation, then separated stable status fields from optional diagnostics and privacy-sensitive data.

**Outcome.** Read-only recommendation completed; no service source, CLI specification, build, or runtime behavior changed.
