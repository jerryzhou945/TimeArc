# Alpha P0/P1 fixes

Goal: close the desktop alpha-facing P0/P1 functional gaps without touching mobile or AI work.

Service side: no new sampling source, schema, or disk-contract field is introduced; storage work is limited to validating JSON string output bytes before writing JSON.

UI side: consume existing CalendarManager, SettingsRepository, and UsageStatManager data more completely by enabling homepage todo completion, common app display names/classification, and 12/24h time display.

Expected touch list: qml/desktop/memorylake, qml/desktop/pages, src/services/usage_stat_manager.cpp, src/service/windows/storage/usage_storage.c, docs/implementation-backlog.md, .harness/state/open-issues.md, docs/*report.md.

Do-not-touch list: frozen schema/ABI/path headers, top-level CMake files, mobile pages, AI card payload rules.
