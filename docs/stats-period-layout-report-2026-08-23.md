# 桌面统计周期布局更新报告

## 目标

减少周、月、年统计中的重复信息和纵向空间浪费，让趋势、分类和全部应用各自只承担一个清晰任务。

## 范围

- 周/月/年改为左右布局：左侧为周期总览与分类分布，右侧为时间趋势；统计内容区仅在小于 900px 时回落单列，常见约 1100px 桌面内容区保持双列。
- 周/月/年的周期总览改为 132px 高横向摘要，移除用于撑高的空白占位；双列区整体高度压缩为 440px。
- 移除周期 Top 应用排行；“所有应用”成为唯一应用明细入口。
- 日视图移除重复的 24H 时间线与 Top 排行，保留应用时钟和今天的组成。
- 应用时钟按 10 分钟窗口选取主分类并绘制有面积的环形扇区，连续同类窗口自动合并，被相同分类夹住的单个零碎窗口会被视觉吸收，不再把断续采样画成密集细线。该处理只作用于时钟展示，分类精确总时长、应用明细和数据库记录不变。
- 悬停临时突出分类；点击任意扇区会放大并锁定该分类的全部弧段，中心同步展示分类总时长、应用数和应用名称，再次点击或点击空白解除。
- 桌面壳层已移除页面顶部的“视觉路径 / 下一步”引导栏，各页面直接使用释放出的垂直空间；新时钟标题、说明、分类和数量摘要均跟随中/英/日语言模式。
- 不修改服务采集、数据库契约、聚合口径或移动端统计页。

## 修改文件

- `qml/desktop/pages/DesktopStatsPage.qml`
- `tests/stats_period_layout_static_test.py`
- `README.md`
- `docs/implementation-backlog.md`
- `.harness/state/open-issues.md`

## 验证

- Track B 预检与修改前完整构建。
- 统计视图模型回归覆盖分类区间并集、并发轨道、精确总时长和分类应用摘要；布局静态回归覆盖左右拓扑、重复区块删除和分类点击锁定。
- 完成后通过 harness 构建、CTest、统计专项测试与 Qt 日志扫描；最新桌面运行未产生 Qt/QML 警告。

## 已知边界

- 周/月/年仍沿用现有真实聚合数据与柱状趋势，不新增统计维度。
- 小于 900px 的窄内容区继续切换为单列，以保证内容可读。
- 分类聚合只改变桌面日视图的呈现，不改服务记录、应用统计明细或移动端时钟。

## 回滚

如需回滚，整体撤销本次统计周期布局提交；不需要迁移或清理任何用户数据。
